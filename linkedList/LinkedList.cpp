// Fill in the definitions below
#include "LinkedList.h"

// Create an empty linked list
LinkedList::LinkedList()
{

}

// Create a linked list that contains the data in the provided array
LinkedList::LinkedList(int initialData[])
{

}

// Add a node to the start of the list
void LinkedList::push(int newValue)
{

}

// Add a node at the provided index (such that LinkedList.get(index) == newValue after this call completes)
void LinkedList::insert(int newValue, int index)
{

}

// Remove a node from the front of the list, returning the value stored in the node
int LinkedList::pop()
{
    return 0;
}

// Remove the node at the specified index, returning the value stored in the node
int LinkedList::remove(int index)
{
    return 0;
}

// Set the value of the node at the specified index
void LinkedList::set(int newValue, int index)
{

}

// Return the value held by the node at the specified index
int LinkedList::get(int index)
{
    return 0;
}
